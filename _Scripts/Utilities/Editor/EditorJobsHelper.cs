using UnityEditor;
using UnityEngine;

namespace com.game.utilities.editor
{
    public static class EditorJobsHelper
    {
        //const string HARDCODED_GROUND_LAYER = "Ground";

        //[MenuItem("Game/Snap to Ground %g")]
        //static void SnapToGround()
        //{
        //    GameObject target = Selection.activeGameObject;

        //    if (target == null)
        //        return;

        //    if (Helpers.Physics.TryGetGroundSnappedPosition(target.transform.position, 
        //        LayerMask.NameToLayer(HARDCODED_GROUND_LAYER),
        //        out Vector3 newPosition))
        //        target.transform.position = newPosition;
        //}
    }
}
